# Web 与 iOS 共用后端：开工入口

按用户要求，执行计划和 Gemini 交接材料放在本 Web 仓库。

**当前入口：[Gemini长程任务](GEMINI_LONG_RUN_HANDOFF.md)**。先修正SH-00语义问题，再连续完成实现与隔离验收，不再逐小任务等待人工确认。原始首轮提示词保留作历史。

- [给 Gemini 的首轮提示词](GEMINI_SHARED_BACKEND_HANDOFF.md)
- [详细执行计划与任务卡](implementation/09_WEB_IOS_SHARED_BACKEND.md)

第一轮从当前仓库执行 SH-00：输出调用清单、兼容字段矩阵、生产写入口和报告，验证后单独提交。只读参考同级 `growdesk-server` 和 `growdesk-ios`，不在本仓库新建后端或复制客户端工程。

后续按任务卡切换到实际负责的仓库：Web 兼容改造在这里；业务 API、数据库迁移、数据导入在 `growdesk-server`；原生账号和同步在 `growdesk-ios`。

## 参考版本和维护规则

这些文档复制自 `growdesk-server` 提交 `d9604d5a773630e81c0bedcc70b7bcf013c64535` 的 `docs/plan/`。交接提示词和09入口已适配本仓库；总计划及01至08作为该版本的背景参考一起保留，避免 Gemini 缺少契约、迁移和验收上下文。

正式接口、数据库、权限与同步规格仍以服务端仓库02/03/07/08及其最新已审核修订为准。参考副本不是另一份独立协议权威；开始实现前比较服务端最新版本，发现差异先更新参考版本和说明，不能让两份方案独立演进。背景文档中的历史目录指令不能覆盖这里明确的首轮任务路径。

复制文档不代表其中的任务已完成。已有部署/迁移证据位于同级服务端仓库；本次未搬动源码、数据库或部署配置。
