[{"path":"components/ui/QuickAiModal.tsx","original":"4a8fab2522786909fb665567866105ed90278edc","target":"bea6fb7eae249fcabeb15ba9098bde67fb1b4dc8","edits":[[47,47,"import { pollJobResult } from \"@/lib/growdesk/job-poll\";\nimport type { ChatPlan } from \"@/lib/growdesk/durable-chat\";\n"],[54,54,"import { loadDailySummaryFromBackend } from \"@/lib/growdesk/daily-summary-client\";\n"],[440,440,"  const [pendingPlan,setPendingPlan]=useState<{runId:string;plan:ChatPlan}|null>(null);\n  const [confirmingPlan,setConfirmingPlan]=useState(false);\n  useEffect(()=>setPendingPlan(null),[baby?.id,currentContextType]);\n  const applyPendingPlan=async()=>{\n    if(!pendingPlan||!baby)return;setConfirmingPlan(true);\n    try{const response=await fetch('/api/ai/chat/confirm',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({runId:pendingPlan.runId,planHash:pendingPlan.plan.planHash,actionIds:pendingPlan.plan.actions.map(a=>a.actionId),babyId:baby.id})});const data=await response.json();if(!response.ok)throw new Error(data.error||'确认失败');setPendingPlan(null);showToast('已确认并保存记录');await useBabyStore.getState().refreshAll();}\n    catch(error){showToast(error instanceof Error?error.message:'确认失败');}finally{setConfirmingPlan(false);}\n  };\n"],[456,464,"      const identity=useBabyStore.getState();\n      if(!identity.user)return;\n      const data=await loadDailySummaryFromBackend({babyId:baby.id,userId:identity.user.id,generate:force});\n      if(useBabyStore.getState().baby?.id===baby.id&&useBabyStore.getState().user?.id===identity.user.id)setDailySummaryData(data);\n\n    } catch(error) {\n      showToast(error instanceof Error?error.message:\"日报读取失败\");\n"],[505,505,"      setPendingPlan(data.pendingPlan&&data.runId?{runId:data.runId,plan:data.pendingPlan}:null);\n"],[689,689,"          const recordedScope=useBabyStore.getState();\n          if(!recordedScope.baby||!recordedScope.user)throw new Error('请先选择宝宝');\n          fd.append('babyId',recordedScope.baby.id);\n          fd.append('clientRequestId',crypto.randomUUID());\n"],[691,692,"          let data = await res.json().catch(() => ({}));\n"],[693,693,"          if(res.status===202){data=await pollJobResult(String(data.jobId),recordedScope.baby.id);}\n          if(useBabyStore.getState().baby?.id!==recordedScope.baby.id||useBabyStore.getState().user?.id!==recordedScope.user.id)throw new Error('账号或宝宝已切换，识别结果保留在原账号任务中');\n          if(typeof data.text!=='string')throw new Error('识别结果缺少文本');\n"],[784,784,"            clientMessageId: crypto.randomUUID(),\n"],[787,787,"        if(!res.ok){const error=await res.json().catch(()=>({}));throw new Error(error.error||`请求失败 HTTP ${res.status}`);}\n"],[810,810,"                if(data.plan&&typeof data.runId==='string')setPendingPlan({runId:data.runId,plan:data.plan});\n                if(data.pending)showToast(data.message||'任务仍在后端处理中');\n                if(data.error)showToast(String(data.error));\n"],[966,966,"        if(!res.ok){const error=await res.json().catch(()=>({}));throw new Error(error.error||`请求失败 HTTP ${res.status}`);}\n"],[989,989,"                if(data.plan&&typeof data.runId==='string')setPendingPlan({runId:data.runId,plan:data.plan});\n                if(data.pending)showToast(data.message||'任务仍在后端处理中');\n                if(data.error)showToast(String(data.error));\n"],[1073,1073,"      setPendingPlan(data.pendingPlan&&data.runId?{runId:data.runId,plan:data.pendingPlan}:null);\n"],[1888,1888,"              {pendingPlan&&(<section className=\"p-3 border-t border-primary/20 bg-card\" aria-label=\"待确认的记录变更\">\n                <p className=\"text-sm font-semibold\">以下变更尚未写入，请核对</p>\n                {pendingPlan.plan.actions.map(action=><details key={action.actionId} className=\"text-xs my-2\"><summary>{action.summary}</summary><pre className=\"whitespace-pre-wrap\">{JSON.stringify(action.payload,null,2)}</pre></details>)}\n                <button type=\"button\" disabled={confirmingPlan} onClick={()=>void applyPendingPlan()} className=\"px-3 py-2 rounded bg-primary text-white disabled:opacity-50\">确认保存</button>\n                <button type=\"button\" disabled={confirmingPlan} onClick={async()=>{try{const res=await fetch('/api/ai/chat/cancel',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({runId:pendingPlan.runId,babyId:baby?.id})});if(!res.ok)throw new Error('取消失败');setPendingPlan(null);}catch(e){showToast(e instanceof Error?e.message:'取消失败');}}} className=\"px-3 py-2\">不执行</button>\n              </section>)}\n"]]},{"path":"components/ui/VoiceConfirmEntry.tsx","original":"7713c886e0c1eeb0e4d3e50c8839913330d4c7d4","target":"382d2e73ea7f3404f446202a3c37709349dac42b","edits":[[2,3,"import React, { useCallback, useRef, useState, useEffect } from \"react\";\n"],[8,8,"\nimport { pollJobResult } from \"@/lib/growdesk/job-poll\";\nimport type { ChatPlan } from \"@/lib/growdesk/durable-chat\";\n"],[16,16,"  const userId = useBabyStore((s) => s.user?.id);\n  const [plan, setPlan] = useState<{runId:string;plan:ChatPlan}|null>(null);\n  const [savingPlan,setSavingPlan] = useState(false);\n  useEffect(()=>{setPlan(null);setActions([]);setOpen(false);},[userId,baby?.id,contextType]);\n"],[50,50,"    const originalUser = userId, originalBaby = baby?.id;\n    const sameIdentity = () => useBabyStore.getState().user?.id === originalUser && useBabyStore.getState().baby?.id === originalBaby;\n"],[54,55,"        body: JSON.stringify({ text, contextType, babyId: baby?.id, clientMessageId: crypto.randomUUID() }),\n"],[58,58,"      if (res.status === 202) {\n        const deadline = Date.now() + 180000;\n        while (sameIdentity() && Date.now() < deadline) {\n          const response = await fetch(`/api/ai/chat?sessionId=${encodeURIComponent(data.sessionId)}&babyId=${encodeURIComponent(originalBaby!)}`, { cache: \"no-store\" });\n          const run = await response.json();\n          if (!response.ok) throw new Error(run.error || \"读取语音提案失败\");\n          if (run.status === \"awaiting_confirmation\") {\n            if (sameIdentity()) { setPlan({runId:run.runId,plan:run.plan}); setTranscript(text); setOpen(true); } return;\n          }\n          if (run.status === \"succeeded\") { if(sameIdentity()){setTranscript(text);setActions([]);setOpen(true);} return; }\n          if (run.status === \"failed\" || run.status === \"cancelled\") throw new Error(\"语音提案处理失败，可在会话历史中查看\");\n          await new Promise(resolve=>setTimeout(resolve,1000));\n        }\n        if(sameIdentity())showToast(\"任务已保存，可在对应 AI 会话中继续查看\");\n        return;\n      }\n      if (!sameIdentity()) return;\n"],[70,70,"  };\n\n  const confirmPlan = async () => {\n    if (!plan || !baby?.id || savingPlan) return;\n    const currentUser=userId,currentBaby=baby.id;\n    setSavingPlan(true);\n    try {\n      const response=await fetch(\"/api/ai/chat/confirm\",{method:\"POST\",headers:{\"content-type\":\"application/json\"},body:JSON.stringify({runId:plan.runId,babyId:currentBaby,planHash:plan.plan.planHash,actionIds:plan.plan.actions.map(a=>a.actionId)})});\n      const result=await response.json();if(!response.ok)throw new Error(result.error||\"确认失败\");\n      if(useBabyStore.getState().user?.id!==currentUser||useBabyStore.getState().baby?.id!==currentBaby)return;\n      setPlan(null);setOpen(false);await useBabyStore.getState().refreshAll();showToast(\"记录已保存\");\n    }catch(error){showToast(error instanceof Error?error.message:\"确认失败\");}finally{setSavingPlan(false);}\n"],[91,91,"          const identity = useBabyStore.getState();\n          if (!identity.user?.id || !identity.baby?.id) throw new Error(\"请先选择宝宝\");\n          const originalUser = identity.user.id, originalBaby = identity.baby.id;\n"],[92,92,"          fd.append(\"babyId\", originalBaby); fd.append(\"clientRequestId\", crypto.randomUUID());\n"],[96,97,"          const result = res.status === 202 ? await pollJobResult<{text:string}>(data.jobId, originalBaby) : data;\n          if (useBabyStore.getState().user?.id !== originalUser || useBabyStore.getState().baby?.id !== originalBaby) return;\n          await parseText(String(result.text || \"\").trim());\n"],[181,182,"            {plan ? (\n              <div className=\"space-y-3\">\n                <p className=\"text-sm\">以下内容尚未写入，请逐项核对后确认。</p>\n                {plan.plan.actions.map(action=><details key={action.actionId} className=\"border rounded-lg p-3\"><summary>{action.summary}</summary><pre className=\"text-xs whitespace-pre-wrap\">{JSON.stringify(action.payload,null,2)}</pre></details>)}\n                <button type=\"button\" disabled={savingPlan} onClick={()=>void confirmPlan()} className=\"w-full rounded-xl bg-primary text-white p-3\">{savingPlan?\"正在保存\":\"确认并保存以上记录\"}</button>\n              </div>\n            ) : actions.length === 0 ? (\n"]]},{"path":"lib/agent/session-runner.ts","original":"2364acb15f17d821976ecfca60363a9c9041a4a4","target":"244d5098f12afbbb574e78ac161189b06b2f90e9","edits":[[65,66,""],[76,79,""]]},{"path":"lib/ai-daily-summary.ts","original":"4473ef984440cdbbce70e901727284bcbeb4043c","target":"1175f820c39e29a2639787b9b65bb69f328a2601","edits":[[0,0,"import { fetchScopedPages } from \"@/lib/growdesk/page-list\";\nimport { pathId } from \"@/lib/growdesk/bridge-protocol\";\n"],[92,100,"      fetchLegacyRecordList<GrowDeskFeedingRecord>(growdeskFetch, token, ctx.babyId, dateQuery, \"feeding\"),\n      fetchLegacyRecordList<GrowDeskSleepRecord>(growdeskFetch, token, ctx.babyId, dateQuery, \"sleep\"),\n      fetchLegacyRecordList<GrowDeskDiaperRecord>(growdeskFetch, token, ctx.babyId, dateQuery, \"diaper\"),\n      fetchLegacyRecordList<GrowDeskFoodRecord>(growdeskFetch, token, ctx.babyId, dateQuery, \"food\"),\n      familyDayBounds(growdeskFetch, token, ctx.babyId, date),\n"],[108,128,"    const [supplements, growth, reports] = await Promise.all([\n      fetchLegacyRecordList<{id:string;occurredAt:string;supplementName:string;amount:string|null}>(growdeskFetch,token,ctx.babyId,dateQuery,\"supplement\"),\n      fetchScopedPages<{id:string;babyId:string;measurementDate:string;weightKg:string|null;heightCm:string|null;headCircumferenceCm:string|null}>(growdeskFetch,token,`/api/v1/babies/${pathId(ctx.babyId)}/growth-measurements`,ctx.babyId),\n      fetchScopedPages<{id:string;babyId:string;reportDate:string}>(growdeskFetch,token,`/api/v1/babies/${pathId(ctx.babyId)}/medical-reports`,ctx.babyId),\n    ]);\n    supplementRecords = supplements.map(record=>({ id:record.id,time:formatIsoToLocalTime(record.occurredAt),\n      dose: 0, unitName: record.amount, product:{name:record.supplementName} }));\n    const measurement=growth.find(record=>record.measurementDate===date);\n    if(measurement) growthRecords={weightKg:measurement.weightKg===null?null:Number(measurement.weightKg),heightCm:measurement.heightCm===null?null:Number(measurement.heightCm),headCircumferenceCm:measurement.headCircumferenceCm===null?null:Number(measurement.headCircumferenceCm)};\n    medicalReports=reports.filter(record=>record.reportDate===date).length;\n"]]},{"path":"lib/growdesk/ai-jobs.ts","original":"b109200941d6814be3ea519859c3a3512c1e1cbb","target":"757471c47469f85018ba941d2203aab3c5d64a9d","edits":[[0,264,"import {growdeskFetch} from \"./client\";\nimport {createBackgroundClient} from \"./background-client\";\nexport type {BffAiJob} from \"./background-client\";\nexport const bffAiJobStore=createBackgroundClient(growdeskFetch);\n"]]}]